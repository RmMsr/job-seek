# Progress ledger — cv-tailoring-iteration

Plan: docs/superpowers/plans/2026-09-15-cv-tailoring-iteration.md
Base before implementation: 20a08fa

- [x] Task 1: Widen cv_versions.action, add note column (commits bff6062..4bd6c96, review clean)
      - Minor (deferred to final review): migration's CREATE INDEX omits IF NOT EXISTS (harmless, DROP TABLE clears the old index first; inconsistent with top-level DDL's convention)
- [x] Task 2: note support in _record_version / upsert_job_cv (commits 5a75434..fb2580f, review clean)
      - Minor (deferred): no direct unit test of parent_id_override taking precedence (brief explicitly defers this to Task 3); unexplained pytest warning in full-suite output, likely pre-existing; upsert_job_cv silently drops note when tailored_cv isn't also passed (matches brief's contract)
- [x] Task 3: reset_job_cv_to_base query function (commits 60e5ad1..0b9027b, review clean)
      - Minor (deferred): resolve_base_cv/resolve_base_version_id each independently query the base version; safe under this app's single-instance non-concurrent usage, could drift under concurrent writes in theory
- [x] Task 4: POST /jobs/{job_id}/cv/reset-to-base route (commits 4785c9d..04e0134, review clean, no findings)
- [x] Task 5: Relabel Update -> Apply tailoring plan, add Reset to base CV button (commits bf444f7..4641b82, review clean, no findings)
- [x] Task 6: History shows action label + note, reordered (commits 9d6cf6a..dca5c4b, review clean)
      - Minor (deferred): no test directly covers manual_edit/reset labels or a missing-segment case (no stray separator) — brief's own prescribed test doesn't cover this either
- [x] Task 7: Apply tailoring plan iterates from the current tailored CV (commits e6449cc..5b1fd6b, review clean)
      - Minor (deferred): new check_guardrails test relies on positional args[3] with a comment rather than kwargs/introspection — matches existing test-file convention, not a regression
- [x] Task 8: Record applied edit-scope as a note on Applied plan versions (commits 1127a8e..eab9647, review clean)
      - Minor (deferred): test_generate_records_applied_scope_note has two dead insert_scope_option calls that don't affect the assertion (seeded DEFAULT_SCOPE_OPTIONS ids 1/2 already resolve to "correct"/"choose") — misleading but not a functional defect

Final whole-branch review (opus, commits de9b5d6..e516d56): 8/8 tasks compose correctly, migration verified end-to-end on a regressed pre-branch DB copy, 1865 tests green. One Important finding: "Apply tailoring plan" iterating from the current draft (Task 7) meant the "Outdated" freshness badge could wrongly flip to "Up to date" after a base-CV edit, since the run never actually reads the new base. User decided: keep iterating from current always (no auto-fallback); stop stamping base_hash/base_cv_snapshot on a run that didn't consume live base; differentiate the badge text ("Base changed" vs "Outdated") so the user knows which remedy applies; simplify "base changed" detection to base_cv content only (base_instruction/base_guardrails are always applied live regardless of source document, so tracking them for draft staleness was never meaningful).
- [x] Fix round 1 (commit 5421b59): narrowed _base_hash to base_cv-only, split staleness into _draft_staleness_reason ("base"/"plan"/None), gated base_hash/base_cv_snapshot stamping on consumed_base, differentiated badge text, plus dead-test-line/migration-index/stale-button-copy/reset-button-gating/job-event-text cleanups. 1867 passed.
- Re-review (opus, commits e516d56..5421b59): confirmed the Important finding genuinely fixed (traced 3 scenarios at runtime). Found 2 NEW Important issues introduced by the narrowing: (1) _guardrail_status was piggybacking on the old 3-field _base_hash for "did the guardrail rule set change" — narrowing to base_cv-only silently broke guardrail-findings staleness detection (empirically confirmed: swapping the whole guardrail set left the badge "Up to date" over stale findings). (2) reset_job_cv_to_base never stamped base_hash/base_cv_snapshot, so the "Base changed" badge — whose own docstring names Reset as the fix — stayed stuck showing "Base changed" after a reset instead of clearing.
- [x] Fix round 2 (commit f0f758b): new `job_cv.guardrails_hash` column + `_guardrails_hash(settings)` (base_cv + base_guardrails, independent of the draft-staleness _base_hash) + `_guardrails_stale()`, stamped in both the generate and recheck paths; reset_job_cv_to_base now stamps base_hash/base_cv_snapshot from the same resolved base content. Plus 2 more stale "Update CV"/"regenerate" copy fixes (app/routes/tasks.py, _cv_diff_fallback.html). 1869 passed.
  - **Note:** the implementer subagent hit the account's monthly spend limit right before committing (all edits were already correct and complete). Verified the diff directly, ran the full suite myself (`uv run pytest -q`, dangerouslyDisableSandbox for uv's cache lock), and committed as the controller rather than dispatching another subagent, given the active spend limit.
  - Deferred Minor (not fixed): pre-existing job_cv rows will show "Base changed" once after deploy until their next Reset+Apply (base_hash was computed with the old 3-field formula) — acceptable one-time noise per this app's stated no-backwards-compat migration philosophy, but undocumented; `consumed_base` defaults to True (fail-open) rather than being a required kwarg; task-list "Update CV"/`_cv_diff_fallback.html` wording were separately caught and fixed in round 2; no negative test for the has_draft-gated Reset button; no assertion on base_cv_snapshot specifically in the "does not clear base staleness" test (only base_hash).
  - **Not yet done:** a second re-review of fix round 2 (the opus reviewer that found round-2's targets hasn't re-checked this round's fix). Given the spend limit, this is a judgment call for the user: re-review now (may hit the same limit), or treat the passing regression tests + direct diff read as sufficient and proceed to merge.
